function [u, dx, dy] = cavity2(x, y, k)


u = (x - 0.35) .* (y - 0.25) .* exp(-((x - 0.35).^2 + (y - 0.25).^2) / (0.1^2));

dx = (y - 0.25) .* (1 + 200.*(x - 0.35).^2) .* exp(-((x - 0.35).^2 + (y - 0.25).^2) / (0.1^2));

dy = (x - 0.35) .* (1 + 200.*(y - 0.25).^2) .* exp(-((x - 0.35).^2 + (y - 0.25).^2) / (0.1^2));

end
